    "custom/gpuinfo": {
        "exec": " ~/.local/share/bin/gpuinfo.sh",
        "return-type": "json",
        "format": "{}",
        "rotate": ${r_deg},
        "interval": 5, // once every 5 seconds
        "tooltip": true,
        "max-length": 1000,
        "on-click": "~/.local/share/bin/gpuinfo.sh --toggle",
    },

    "custom/gpuinfo#nvidia": {
        "exec": " ~/.local/share/bin/gpuinfo.sh --use nvidia ",
        "return-type": "json",
        "format": "{}",
        "rotate": ${r_deg},
        "interval": 5, // once every 5 seconds
        "tooltip": true,
        "max-length": 1000,
    },

    "custom/gpuinfo#amd": {
        "exec": " ~/.local/share/bin/gpuinfo.sh --use amd ",
        "return-type": "json",
        "format": "{}",
        "rotate": ${r_deg},
        "interval": 5, // once every 5 seconds
        "tooltip": true,
        "max-length": 1000,
    },

    "custom/gpuinfo#intel": {
        "exec": " ~/.local/share/bin/gpuinfo.sh --use intel ",
        "return-type": "json",
        "format": "{}",
        "rotate": ${r_deg},
        "interval": 5, // once every 5 seconds
        "tooltip": true,
        "max-length": 1000,
    },

