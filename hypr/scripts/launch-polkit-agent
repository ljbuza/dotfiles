#!/usr/bin/env bash

for agent in \
  /usr/lib/mate-polkit/polkit-mate-authentication-agent-1 \
  /usr/lib/polkit-mate/polkit-mate-authentication-agent-1; do
  if [[ -x "$agent" ]]; then
    exec "$agent"
  fi
done

printf 'No MATE polkit authentication agent was found.\n' >&2
